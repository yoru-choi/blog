---
title: "841. Keys and Rooms"
published: false
description: 
tags: 
cover_image:
series: "leetcode"
---



my solution
```java
class Solution {
    public boolean canVisitAllRooms(List<List<Integer>> rooms) {

        var hashRooms = Hash()
        var keys= [0]

        for(var i = 0; i<room.length; i++){
            for(var j=0; j < keys.lenth; j++){
                hashRooms.set(keys[j], 1)       
            }
                keys = rooms[j]
        }

        for(var i = 0; i<room.length; i++){
            if(hashRooms.value(i)==0){
                return false
            }
        }

        return true
    }
}
```



gpt solve
```java
import java.util.*;

class Solution {
    public boolean canVisitAllRooms(List<List<Integer>> rooms) {
        int n = rooms.size();
        boolean[] seen = new boolean[n];
        dfs(0, rooms, seen);

        for (boolean v : seen) if (!v) return false;
        return true;
    }

    private void dfs(int room, List<List<Integer>> rooms, boolean[] seen) {
        if (seen[room]) return;
        seen[room] = true;
        for (int key : rooms.get(room)) {
            if (!seen[key]) dfs(key, rooms, seen);
        }
    }
}

```